/**
 * MCP transport adapter — publishes compiled tools through Spring AI.
 *
 * <p>This package is the only place that should depend on Spring AI MCP types.
 * Connectors and the governance pipeline remain transport-neutral.
 */
package dev.mytechprofile.gateway.transport;
