/**
 * Governance invocation pipeline.
 *
 * <p>The pipeline adds validation, authorization, quota, trusted-context injection, approval,
 * output shaping, and audit around the same entry point.
 */
package dev.mytechprofile.gateway.pipeline;
