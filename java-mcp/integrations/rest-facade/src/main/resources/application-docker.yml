server:
  address: 0.0.0.0
  port: ${SERVER_PORT:9082}

spring:
  ai:
    mcp:
      client:
        type: SYNC
        request-timeout: 30s
        streamable-http:
          connections:
            gateway:
              url: ${SPRING_AI_MCP_CLIENT_STREAMABLE_HTTP_CONNECTIONS_GATEWAY_URL:http://gateway:8080}
              endpoint: ${SPRING_AI_MCP_CLIENT_STREAMABLE_HTTP_CONNECTIONS_GATEWAY_ENDPOINT:/mcp}
