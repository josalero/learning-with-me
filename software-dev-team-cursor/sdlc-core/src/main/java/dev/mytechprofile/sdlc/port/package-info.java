/**
 * Replaceable adapters for files, processes, git, and run storage.
 *
 * <p><strong>Key scenarios:</strong> run an allowlisted test command; read a file inside the
 * path jail; persist numbered artifacts under {@code runs/<id>/}.
 */
package dev.mytechprofile.sdlc.port;
