/**
 * LangChain4j {@code @Agent} role interfaces. Each method returns a domain record so loops can
 * branch on typed fields.
 *
 * <p><strong>Key scenarios:</strong> Product Owner writes a brief; Developer implements with
 * tools; QA scores each acceptance criterion.
 */
package dev.mytechprofile.sdlc.agent;
