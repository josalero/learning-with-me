/**
 * Role ports and Spring AI ChatClient implementations for the research pipeline.
 *
 * <p>Each role is a single-method interface (ISP). Implementations must not depend on {@code api}.
 */
package dev.mytechprofile.research.springai.agents;
